<progress percent="{{percent}}" stroke-width="5" />
<view>
  <view wx:for="{{chapters}}" wx:for-item="chapter">
    <navigator url="../show/show?id={{id}}&path={{chapter.path}}">
      <text>{{chapter.level}} </text>
      <text>{{chapter.title}}</text>
    </navigator>
  </view>
</view>
